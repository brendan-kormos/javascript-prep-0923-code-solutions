const languages = ['html', 'css', 'javascript', 'react', 'node', 'postgres'];

console.log('languages array', languages);

const firstElement = languages[0];
console.log('firstElement value:', firstElement);

const thirdElement = languages[2];
console.log('thirdElementValue:', thirdElement);

const length = languages.length;
console.log('The length of the array languages:', length);

const lastElement = languages[languages.length - 1];
console.log('The last element of the languages array is:', lastElement);
